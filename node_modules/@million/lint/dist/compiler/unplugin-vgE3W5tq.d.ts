import { ParserOptions } from '@babel/core';

interface Size {
  size: number;
  filename: string;
  source: string;
}

interface Options {
    plugins?: ParserOptions["plugins"];
    filter?: {
        include?: string | RegExp | (string | RegExp)[];
        exclude?: string | RegExp | (string | RegExp)[];
    };
    filename?: string;
    rsc?: boolean;
    dev?: "debug" | boolean;
    legacyHmr?: boolean;
    framework?: string;
    source?: string;
    bundleSizes?: Record<string, Size[]>;
}

export type { Options as O };
